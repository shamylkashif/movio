import 'dart:ui';

const background = Color(0xFF181818);  // Darker gray, close to black
const primaryRed = Color(0xFFD32F2F);  // Main red color for highlights
const white = Color(0xFFFFFFFF);       // White for text/icons
const lightGray = Color(0xFFBDBDBD);   // Light gray for placeholders
const darkGray = Color(0xFF83829A);    // Dark gray for secondary text
const yellow = Color(0xFFFFD700);      // Yellow for ratings or highlights
